# Local Postgres (docker-compose up) — or swap for an InsForge hosted Postgres URL
DATABASE_URL=postgres://postgres:postgres@localhost:5432/agent_engine

PORT=4000

# Optional — engine runs in demo/mock mode without this (free OpenRouter models supported)
OPENROUTER_API_KEY=
